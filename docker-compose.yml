version: '3'

services:
  web:
    build: .
    volumes:
      - ./api_py_files:/app
    ports:
      - "9000:9000"
#    stdin_open: true # docker run -i
#    tty: true        # docker run -t
#    command: /bin/bash
